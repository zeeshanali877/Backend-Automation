{
  "dependencies": {
    "cypress": "^12.9.0"
  },
  "name": "backend-automation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:api": "cypress run --spec cypress/integration/api/*.js --reporter mochawesome --reporter-options reportDir=cypress/reports/mochawesome-report"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress-image-snapshot": "^4.0.1",
    "cypress-multi-reporters": "^1.6.3",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "start-server-and-test": "^2.0.0"
  }
}
