version: 2.1
jobs:
  build:
    docker:
      - image: cypress/base:14.16.0
    working_directory: ~/app
    steps:
      - checkout
      - run: yarn install
      - run: yarn cy:verify
      - run:
          name: Run API tests
          command: yarn cy:run --spec cypress/integration/api/*.spec.js